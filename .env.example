# Copy this file to .env and fill in ONE key. .env is gitignored — never commit keys.

# Free tier — get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Paid alternative (takes precedence if both are set)
# ANTHROPIC_API_KEY=

# Optional overrides:
# PA_MODEL=gemini-2.5-flash
# PA_MODE=offline
